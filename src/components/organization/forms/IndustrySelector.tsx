// IndustrySelector — UI scaffold. Compose real markup as the feature ships.
export const IndustrySelector = () => {
  return (
    <div data-component="IndustrySelector" />
  );
};

export default IndustrySelector;
