// SearchOrganization — UI scaffold. Compose real markup as the feature ships.
export const SearchOrganization = () => {
  return (
    <div data-component="SearchOrganization" />
  );
};

export default SearchOrganization;
