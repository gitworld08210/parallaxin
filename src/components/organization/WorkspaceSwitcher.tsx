// WorkspaceSwitcher — UI scaffold. Compose real markup as the feature ships.
export const WorkspaceSwitcher = () => {
  return (
    <div data-component="WorkspaceSwitcher" />
  );
};

export default WorkspaceSwitcher;
