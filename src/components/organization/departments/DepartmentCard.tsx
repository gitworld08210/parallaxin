// DepartmentCard — UI scaffold. Compose real markup as the feature ships.
export const DepartmentCard = () => {
  return (
    <div data-component="DepartmentCard" />
  );
};

export default DepartmentCard;
