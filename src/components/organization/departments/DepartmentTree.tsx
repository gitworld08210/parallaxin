// DepartmentTree — UI scaffold. Compose real markup as the feature ships.
export const DepartmentTree = () => {
  return (
    <div data-component="DepartmentTree" />
  );
};

export default DepartmentTree;
