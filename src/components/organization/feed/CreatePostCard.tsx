// CreatePostCard — UI scaffold. Compose real markup as the feature ships.
export const CreatePostCard = () => {
  return (
    <div data-component="CreatePostCard" />
  );
};

export default CreatePostCard;
