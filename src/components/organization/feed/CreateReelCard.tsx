// CreateReelCard — UI scaffold. Compose real markup as the feature ships.
export const CreateReelCard = () => {
  return (
    <div data-component="CreateReelCard" />
  );
};

export default CreateReelCard;
