// CreateStoryCard — UI scaffold. Compose real markup as the feature ships.
export const CreateStoryCard = () => {
  return (
    <div data-component="CreateStoryCard" />
  );
};

export default CreateStoryCard;
