// RecentActivity — UI scaffold. Compose real markup as the feature ships.
export const RecentActivity = () => {
  return (
    <div data-component="RecentActivity" />
  );
};

export default RecentActivity;
