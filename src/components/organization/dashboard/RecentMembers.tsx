// RecentMembers — UI scaffold. Compose real markup as the feature ships.
export const RecentMembers = () => {
  return (
    <div data-component="RecentMembers" />
  );
};

export default RecentMembers;
