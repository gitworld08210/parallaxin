// RecentProjects — UI scaffold. Compose real markup as the feature ships.
export const RecentProjects = () => {
  return (
    <div data-component="RecentProjects" />
  );
};

export default RecentProjects;
