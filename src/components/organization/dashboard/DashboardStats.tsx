// DashboardStats — UI scaffold. Compose real markup as the feature ships.
export const DashboardStats = () => {
  return (
    <div data-component="DashboardStats" />
  );
};

export default DashboardStats;
