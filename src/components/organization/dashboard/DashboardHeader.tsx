// DashboardHeader — UI scaffold. Compose real markup as the feature ships.
export const DashboardHeader = () => {
  return (
    <div data-component="DashboardHeader" />
  );
};

export default DashboardHeader;
