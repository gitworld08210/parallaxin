// InviteMemberModal — UI scaffold. Compose real markup as the feature ships.
export const InviteMemberModal = () => {
  return (
    <div data-component="InviteMemberModal" />
  );
};

export default InviteMemberModal;
