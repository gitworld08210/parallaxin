// MemberRoleBadge — UI scaffold. Compose real markup as the feature ships.
export const MemberRoleBadge = () => {
  return (
    <div data-component="MemberRoleBadge" />
  );
};

export default MemberRoleBadge;
