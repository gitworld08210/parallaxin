// PermissionCard — UI scaffold. Compose real markup as the feature ships.
export const PermissionCard = () => {
  return (
    <div data-component="PermissionCard" />
  );
};

export default PermissionCard;
