// LoadingWorkspace — UI scaffold. Compose real markup as the feature ships.
export const LoadingWorkspace = () => {
  return (
    <div data-component="LoadingWorkspace" />
  );
};

export default LoadingWorkspace;
