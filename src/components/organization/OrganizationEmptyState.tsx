// OrganizationEmptyState — UI scaffold. Compose real markup as the feature ships.
export const OrganizationEmptyState = () => {
  return (
    <div data-component="OrganizationEmptyState" />
  );
};

export default OrganizationEmptyState;
